    # for name, param in net.named_parameters():
    #     if "delta_th" in name and 'classifier' in name:
    #         param.register_hook(lambda grad: grad.clamp(min=-0.001, max=0.001))

for name, param in net.na:
    if     